import path from "path";

export const MEMORY_SESSION_CARDS = 15;
export const LEETCODE_SESSION_CARDS = 5;

export const DATA_PATH = path.join(process.cwd(), "data", "progress.json");
export const CONFIG_PATH = path.join(process.cwd(), "data", "config.json");
